# BTObot dependencies
# Install with:  pip install -r requirements.txt

# --- Frontend -----------------------------------------------------------
chainlit>=1.3.0

# --- LangChain core stack -----------------------------------------------
langchain>=0.2.16
langchain-community>=0.2.16
langchain-ollama>=0.1.3
langchain-chroma>=0.1.4
langchain-text-splitters>=0.2.4

# --- Vector store --------------------------------------------------------
chromadb>=0.5.5

# --- Reranker (ONNX, CPU-friendly, no PyTorch required) ------------------
flashrank>=0.2.9

# --- Web scraping --------------------------------------------------------
playwright>=1.40.0          # headless Chromium for JS-rendered pages (HDB site)
beautifulsoup4>=4.12.3
lxml>=5.3.0
requests>=2.32.0

# --- Utilities -----------------------------------------------------------
python-dotenv>=1.0.1
